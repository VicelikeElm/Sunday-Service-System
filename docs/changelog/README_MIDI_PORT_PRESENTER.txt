SSS — PRESENTER MIDI PORT UPDATE
================================

The default MIDI port for the Scripture controls is now:

  Presenter

This matches the loopMIDI port shown on the church PC.

The existing mappings remain unchanged:

  Channel: 10
  NEXT:    C3 / Note 60 / Velocity 126
  BACK:    D3 / Note 62 / Velocity 126

No configuration file is included or overwritten.
